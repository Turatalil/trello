import { createSlice } from "@reduxjs/toolkit"

const initialState = {
    users: []
}

const buttonViaMainSlice = createSlice({
    name: "button",
    initialState,
    reducers: {
        addSpisok(state, action){
            
        }
    }
})